// import { httpFetchOwned } from "@/api/fetcher";

// export class ThirdWebEngine {
//   public static isNFTOwned = async (reciever: string) => {
//     const response = await httpFetchOwned(reciever);
//     return response.result.length > 0;
//   };

//   public static mintNFT = async (reciever: string) => {
//     const response = await mintNFT(reciever);
//     return response;
//   };
// }
